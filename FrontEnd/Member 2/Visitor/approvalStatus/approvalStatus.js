// Approval Status - search & filter
document.addEventListener("DOMContentLoaded", () => {
  initTableSearchFilter({
    searchInput: document.getElementById("statusSearch"),
    filterSelects: [document.getElementById("statusFilter")],
    tableBody: document.getElementById("statusTableBody"),
    emptyStateEl: document.getElementById("statusEmptyState"),
  });
});


function updateGreeting() {
  const elements = document.querySelectorAll(".welcome-hi");
  if (!elements.length) return;
  const hour = new Date().getHours();
  let greetingText = "Good morning";
  if (hour >= 12 && hour < 17) {
    greetingText = "Good afternoon";
  } else if (hour >= 17) {
    greetingText = "Good evening";
  }
  elements.forEach((el) => {
    const text = el.textContent || "";
    const hasComma = text.trim().endsWith(",");
    el.textContent = greetingText + (hasComma ? "," : "");
  });
}

(function() { if (document.readyState === "loading") { document.addEventListener("DOMContentLoaded", updateGreeting); } else { updateGreeting(); } })();
